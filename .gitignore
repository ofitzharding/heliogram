# Captures and rendered video: large, and regenerable from the code.
captures/
demo/*.mp4
demo/_tx_*
demo/_countdown*
demo/payload*.png
demo/recovered.png
*.dat.npy

# Generated transmit dirs (frames.bin runs to tens of MB). The published web
# receiver's slim metadata is the one exception: it ships with the site.
demo/webtx*/*
!demo/webtxweb/rxmeta.slim.json

# Build products.
__pycache__/
*.pyc
src/*.dylib
experiments/smear-test/smeartest
